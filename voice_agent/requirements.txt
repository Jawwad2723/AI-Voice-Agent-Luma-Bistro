# Reservation API client / tests (Python 3.10+)
httpx>=0.27.0
pydantic>=2.7.0
python-dotenv>=1.0.1
pytest>=8.2.0
pytest-asyncio>=0.23.0
# Pin binary wheel–friendly cryptography (avoids local Rust build)
cryptography>=42,<45

# LiveKit voice agent
livekit-agents[silero,deepgram,google,elevenlabs]~=1.0
livekit-api>=1.0.0

# Optional: local token server for browser client
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
